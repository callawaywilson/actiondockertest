name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  ENV_KEY: 
    description: 'Environment Key'
    required: true
    default: 'ENV Key Missing'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.ENV_KEY }}