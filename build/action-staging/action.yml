name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  target: 
    description: 'Target build'
    required: true
    default: 'NoTarget'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.target }}